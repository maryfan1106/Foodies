name: Foodies
description: Always know where to eat!

version: 0.0.1

environment:
  sdk: ">=2.3.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: any
  http: any
  shared_preferences: any
  google_fonts: any
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - .env
    - google_fonts/
